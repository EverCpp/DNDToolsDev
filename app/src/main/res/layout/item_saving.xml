<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/savingItemLayout"
    android:layout_width="match_parent"
    android:layout_height="@dimen/char_saving_height">

    <androidx.appcompat.widget.AppCompatCheckBox
        android:id="@+id/savingItemCheckBox"
        style="@style/label_style"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:button="@drawable/shape_checkbox"
        android:checked="false"
        android:focusable="auto"
        android:maxLines="1"
        android:paddingStart="@dimen/control_margin_normal"
        android:paddingEnd="@dimen/control_margin_normal"
        android:singleLine="true"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/savingItemTextBonus"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="TextContrastCheck,TouchTargetSizeCheck"
        tools:text="Saving" />

    <TextView
        android:id="@+id/savingItemTextBonus"
        style="@style/label_style"
        android:layout_width="@dimen/char_saving_bonus_width"
        android:layout_height="0dp"
        android:gravity="center"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="+0" />
</androidx.constraintlayout.widget.ConstraintLayout>